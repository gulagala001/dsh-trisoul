//#region src/index.ts
/**
* TriSoul Settings plugin, server half.
* Pure UI plugin: the empty apply exists so the plugin appears in the host
* Cordis loader; the browser half ships via exports["./client"].
*/
function apply() {}
//#endregion
export { apply };
